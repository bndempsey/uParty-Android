<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>
    <style name="datepicker" parent="@android:style/Widget.DeviceDefault.DatePicker">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <item name="android:headerBackground">@color/colorPrimary</item>

    </style>
    <style name="mainStyle" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:datePickerStyle">@style/datepicker</item>
        <item name="android:datePickerDialogTheme">@style/datepicker</item>
        <item name="spinnerStyle">@style/datepicker</item>
        <item name="android:spinnerItemStyle">@style/datepicker</item>
        <item name="android:spinnerStyle">@style/datepicker</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textColorPrimary">#000000</item>
    </style>

</resources>
